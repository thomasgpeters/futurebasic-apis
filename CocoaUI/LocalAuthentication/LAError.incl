/*  LAError.incl - https://developer.apple.com/documentation/laerror?language=objc  Bernie Wylde 20220414  Framework: LocalAuthentication*/#if ndef _DEFINEDINCOCOA#define LAErrorDomain as CFStringRef#define LAError as NSInteger#endifsystem LAError LAErrorAuthenticationFailedsystem LAError LAErrorUserCancel// Authentication was canceled by user (e.g. tapped Cancel button).system LAError LAErrorUserFallback// Authentication was canceled because the user tapped the fallback button (Enter Password).system LAError LAErrorSystemCancel// Authentication was canceled by system (e.g. another application went to foreground).system LAError LAErrorPasscodeNotSet// Authentication could not start because passcode is not set on the device.system LAError LAErrorAppCancel // Authentication was canceled by application (e.g. invalidate was called while authentication was in progress).system LAError LAErrorInvalidContext // LAContext passed to this call has been previously invalidated.system LAError LAErrorBiometryNotAvailable // Authentication could not start because biometry is not available on the device.system LAError LAErrorBiometryNotEnrolled // Authentication could not start because biometry has no enrolled identities.// Authentication was not successful because there were too many failed biometry attempts and// biometry is now locked. Passcode is required to unlock biometry, e.g. evaluating LAPolicyDeviceOwnerAuthenticationWithBiometrics will ask for passcode as a prerequisite.system LAError LAErrorBiometryLockoutsystem LAError LAErrorNotInteractive// Authentication failed because it would require showing UI which has been forbidden by using interactionNotAllowed property.#if _MACOSMAXALLOWED >= 101500system LAError LAErrorWatchNotAvailable// macOS 10.15+ // Authentication could not start because there was no paired watch device nearby.#endif// 101500#if _MACOSMAXALLOWED >= 110200system LAError LAErrorBiometryNotPaired// macOS 11.2+ // Authentication could not start because this device supports biometry only via removable accessories and no accessory has been paired.system LAError LAErrorBiometryDisconnected// macOS 11.2+ // Authentication could not start because this device supports biometry only via removable accessories and the paired accessory is not connected.#endif// 110200#if _MACOSMAXALLOWED >= 120000system LAError LAErrorInvalidDimensions// macOS 12.0+ // Authentication could not start because dimensions of embedded UI are invalid.#endif// 120000