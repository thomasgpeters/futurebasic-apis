CFStringRef stringprint unix( @"ls -A" )          // should have output, so not checkingprint unix( @"banner FB7" )		 //  same as abovestring = unix( @"cd" )if ( fn StringLength( string) ) // not all unix commands return data, so check if in doubt.print stringelseprint @"empty string!"end if// unix statment version when output isn't provided or neededunix( @"cd" )unix(@"touch /Users/yourUserID/Desktop/someFile")  // change 'yourUserID' or use '~' for the pathHandleEvents